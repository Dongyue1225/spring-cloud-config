server:
  port: 8765

feign:
  hystrix:
    enabled: true

eureka:
  client:
    #不注册到Eureka,不在注册中心显示
    registerWithEureka: false
    #Eureka客户端与Eureka服务端的交互地址
    service-url:
      defaultZone: http://localhost:8761/eureka/